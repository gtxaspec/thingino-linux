config VPU_RADIX
    depends on JZ_VPU
    bool "vpu radix client device of jz_vpu"
    default y

if VPU_RADIX
config VPU_RADIX_NUM
    depends on VPU_RADIX
    int "vpu radix node number"
    default 1
endif
